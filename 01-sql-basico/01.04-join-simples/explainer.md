# JOIN simples

`JOIN outra_tabela ON condicao` junta linhas de duas tabelas com base numa condição de igualdade — aqui, `emprestimos.livro_id = livros.id`. Sem o JOIN, você teria o `livro_id` (um número) em vez do título do livro.
