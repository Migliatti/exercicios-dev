# INSERT, UPDATE, DELETE

`INSERT INTO tabela (colunas) VALUES (...)` cria uma linha nova. `UPDATE tabela SET coluna = valor WHERE condicao` muda linhas existentes — sem o `WHERE`, muda a tabela inteira. `DELETE FROM tabela WHERE condicao` remove linhas — mesma regra do `WHERE`.
