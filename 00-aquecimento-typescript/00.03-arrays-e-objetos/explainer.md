# Arrays e objetos

Um array de objetos é como uma tabela em memória: cada objeto é uma "linha", cada propriedade é uma "coluna". `.filter()` devolve só os itens que passam num teste; `.find()` devolve o primeiro item que passa. É o mesmo raciocínio de um `WHERE` em SQL — que você vai ver na próxima seção.
