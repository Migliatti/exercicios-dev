exports.up = (pgm) => {
  // TODO: crie a tabela "livros" com as colunas:
  // id (serial, primary key), titulo (text, obrigatório), autor (text, obrigatório),
  // ano (integer, obrigatório), disponivel (boolean, obrigatório, default true)
};

exports.down = (pgm) => {
  // TODO: desfaça a criação da tabela (dropTable)
};
