# 05.02 — Teste de integração de endpoint

Escreva o teste marcado com `TODO` em `app.test.ts`.

Pré-requisito: o Postgres da raiz precisa estar rodando (`docker compose up -d`, na raiz do repo) com a tabela `livros` existindo.

Rode com:

    npx vitest run problem/app.test.ts

Resultado esperado: `1 passed`.
