# 05.01 — Teste unitário de função pura

Escreva os 3 testes marcados com `TODO` em `validacao.test.ts` (a função `validarLivro` já está pronta em `validacao.ts`).

Rode com:

    npx vitest run problem/validacao.test.ts

Resultado esperado: `3 passed`.
