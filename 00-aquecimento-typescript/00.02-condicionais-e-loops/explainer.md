# Condicionais e loops

`if`/`else` decide entre caminhos; um `for...of` percorre cada item de um array. Combinar os dois é a base de quase toda regra de negócio (ex.: "para cada livro, decida algo com base numa condição").
